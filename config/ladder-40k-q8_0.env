# Intermediate rung for 0014. 32k has been used all session without complaint and 64k
# freezes the desktop; the useful answer is where between them that starts, so the band
# is walked at 8k granularity. Nothing below 32k is re-walked — those cells are not in
# question and each one costs its full ingest.
MODEL_HF="bartowski/Qwen3.8-27B-GGUF:Q4_K_M"
HOST="127.0.0.1"
PORT="8080"
CTX_SIZE="40960"
CACHE_TYPE_K="q8_0"
CACHE_TYPE_V="q8_0"
FLASH_ATTN="auto"
N_GPU_LAYERS="999"
PARALLEL="1"
JINJA="1"
