# Measurement data

Raw rows behind the tables in [../TECH.md](../TECH.md). Committed because they are the
evidence every conclusion in this repo rests on, and because re-measuring is expensive: one
ladder cell costs 13 minutes and a harness sweep costs a day. The hardware is fixed, so these
stay re-runnable — but a conclusion nobody can check without spending that time is one nobody
checks.

## Naming

    <date>-<machine>-<what>.jsonl

The machine is in the filename because a measurement without it is not reusable. Nothing
here should be compared across machines without saying so.

## What is here

| file | rows | what |
|---|---|---|
| `2026-08-17-m2max-32gb-ceiling-ladder.jsonl` | 12 cells + apparatus | Context ladder 8k–64k, two conditions: `unattended` (clean boot, swap zero) and `attended-worked-in` (4 h uptime, 13.7 GB swap already allocated). The worked-in rows carry `instrument: pre-timing` and have no `fill_seconds` — they predate that metric and cannot be compared on speed. |
| `2026-08-17-m2max-32gb-tier1-matrix.jsonl` | 42 runs | Thinking on vs off at model-card sampling, baseline 32k/q8_0, three passes over seven tasks. |
| `2026-08-18-m2max-32gb-0005-sampling.jsonl` | 162 runs | Temperature and top_p swept around each mode's default, plus greedy, one value moved per cell. |
| `2026-08-18-m2max-32gb-0005-toggle.jsonl` | 81 runs | Thinking off, on/low and on/medium, **each at its own model-card sampling** — the first clean comparison across the toggle. Supersedes every earlier thinking-vs-off figure. |
| `2026-08-18-m2max-32gb-0013-calibration-r1.jsonl` | 84 runs | First calibration of the 14-task suite, both thinking modes, three passes, at 32k/q8_0. **Failed its own criterion** — one genuine failure in 78 clean runs, so the suite still cannot rank. Six rows are `fail_truncated_at_cap` and are cap artifacts, not quality: read them with the caps of that run (2048 patch, 1024 tool-call), which have since been raised. |
| `2026-08-18-m2max-32gb-0013-readonly-fixed.jsonl` | 12 runs | `toolcall-constraint-readonly` at all four reasoning levels after its fixture was fixed. Supersedes that task's rows in the two files above, which scored the model for declining to patch a file it had not been shown. |
| `2026-08-18-m2max-32gb-0013-reasoning-effort.jsonl` | 18 runs | The three tasks whose caps bound at `xhigh`, re-run at `low` and `medium` with `reasoning_effort` set explicitly. The first file to carry the level; rows in every earlier file are `xhigh` whether they say so or not. |
| `2026-08-18-m2max-32gb-desktop-band.jsonl` | 5 cells + apparatus | The 32k–64k band re-walked `attended-worked-in` against the desktop rather than the model, with wired memory and WindowServer sampled through each fill. Carries `desktop_verdict`, `wired_peak_gb` and the WindowServer series summary. The apparatus row records the desktop baseline the verdicts were judged against. |
| `2026-08-19-m2max-32gb-0006-mlx-unbounded-cache.jsonl` | 37 runs | The same MLX suite with **no cache bound at all**, which is `mlx_lm`'s default. Free memory reaches 0.00 with swap flat — wired pages cannot be paged out — and 9 runs hit their budget rather than slowing down. The run did not finish, which is why there are 37 rows and not 42. This is the evidence behind "unbounded is not an option" in TECH.md, and it is the first of the three misconfigurations that section counts; the 16-slot file below is the second. |
| `2026-08-19-m2max-32gb-0006-mlx-16-slots.jsonl` | 42 runs | The same MLX run at 16 cache slots. Every depth row fails with free memory at 0.00 and swap flat: evidence that `mlx_lm` allocates cache capacity per slot at startup, not as it fills. |
| `2026-08-19-m2max-32gb-0006-runtime.jsonl` | 84 runs | llama.cpp against MLX, 42 rows each, same model matched by footprint, 0005's settled config, only `-endpoint` differing. MLX at 2 cache slots — the memory-safe configuration. |
| `2026-08-19-m2max-32gb-0008-harness-overhead.jsonl` | 6 harnesses + apparatus | What each harness spends before the user's first word: system prompt and tool definitions, counted by the served tokeniser. No model ran — every harness was pointed at an endpoint that records the request and answers 400, so `model_calls_before_giving_up` counts attempts, not a turn. |
| `2026-08-19-m2max-32gb-0008-residual-traffic.jsonl` | 3 conditions + apparatus | Which hosts a `claude -p` session contacts while doing one real task, measured through a CONNECT proxy that records the host and tunnels TLS untouched. Records hosts, never traffic. The apparatus row carries what the instrument cannot see. |
| `2026-08-19-m2max-32gb-0008-toolcall-messages.jsonl` | 12 runs | The four tool-call fixtures through the **Anthropic Messages path** llama-server converts internally, three passes, served by `config/agent.env`. Sampling and the thinking toggle are the server's on this path and are not on the row; `prompt_per_second` and `gen_per_second` are zero because that endpoint reports no timings. Compare with the `thinking: off` tool-call rows of `2026-08-18-m2max-32gb-0005-toggle.jsonl`. |
| `2026-08-19-m2max-32gb-0010-harness-sweep.jsonl` | 60 runs | Four harnesses over five patch fixtures, three passes, at 65,536 — the one context all four accept, since Hermes refuses anything under 64,000. Every run cold (each harness pointed at a state directory of its own) and bounded at ten minutes. `prompt_tokens`, `cached_tokens` and `completion_tokens` are the server's counters read either side of each run, not the harness's own accounting; `turns` counts the tasks its slot was busy with. Timings on rows whose `swap_delta_mb` grew measured the pager: at this context with an editor resident the machine pages, so read wall only from the clean rows. |
| `2026-08-19-m2max-32gb-0010-offline.jsonl` | 4 runs | The same fixture with the network denied by a sandbox profile, loopback left open. `offline: true` on every row. Under the same profile at the same moment `api.anthropic.com` and `registry.npmjs.org` both failed to connect while the local endpoint answered 200. |
| `2026-08-20-m2max-32gb-0017-curve.jsonl` | 18 runs + 6 fidelity, 3 depths | The same pair at 8k, 16k and 32k of prompt, 3 passes each side per depth, one session per depth so the points are not averaged together. The fixtures are `tasks/depth/decode-*.json`: retrieval bodies at depth with an answer long enough to time — 384 tokens against the 12 the plain retrieval fixtures return, which is too few to divide. Only the first run at each depth pays prefill; the rest hit the prompt cache, which changes nothing for a decode figure since the KV is the same depth either way. Read with the pair file: the ratio falls from 1.40x at 8k to 1.26x at 32k while acceptance holds near 3.97, so what decays is the cost of a verification step and not the drafting. |
| `2026-08-20-m2max-32gb-0017-pair.jsonl` | 54 runs + 2 fidelity | The tier-1 suite through `config/tuned.env` and then `config/mtp-32k.env`, back to back on one machine state, 3 passes each at 0005's settled sampling and 32k. Rows carry `decode_seconds` measured client-side from the gap to the first token, so the ratio is of decode and not of wall; `acceptance_length` is present only on the speculating side, and absent is not zero. The two `kind: fidelity` rows hash fixed greedy probes and are equal, which is the losslessness evidence — they are instrument rows and are not scored as tasks. Read the ratio from the reporter rather than the rows: it drops runs that swapped, runs the machine throttled, and samples past 4x their own side's median. The tool-call tasks are unstreamed by construction and carry no decode figure. |
| `2026-08-20-m2max-32gb-0017-screen.jsonl` | 15 screens | Three speculative-decoding candidates against 0014's desktop verdict at 32k and 49k, plus the baseline they are read against, each judged from a load rather than a full ladder cell. `smoke` is the row that decides: a server can answer `/health` with 200 and still fail every Metal command buffer out of memory, so a config that cannot generate one token is `admissible: false` whatever its memory reads. Rows with `smoke_prompt_tokens: 0` are load-only and the field is absent on the first six, which predate it; the `-filled` rows send a prompt sized to 90% of the context, which is the only way a runtime that allocates KV per request is judged at its profile at all. The two external-mechanism candidates failed with `kIOGPUCommandBufferCallbackErrorOutOfMemory` at both profiles, with a quantised KV cache and without; the `mtp-llamacpp` rows are the target's own MTP head served from the same weights, and they split — admissible at 32k, the same allocator error at 49k, where it fires on the first prefill batch rather than partway through. `condition` is `unattended` throughout: the desktop verdict is `not_applicable` and the compositor series behind each row was kept. The two `mtp-llamacpp` rows plus `mtp-38k-filled` and `mtp-40k-filled` bracket where the winning config stops: 38,912 ingests 35,020 tokens and answers at 22.28 GB, 40,960 refuses on the first prefill batch. |
| `2026-08-20-m2max-32gb-0018-eviction.jsonl` | 5 conditions, one row per request | A fixed conversation replayed on `config/agent.env` at two depths — ~28k, the band 0018's observation came from, and ~44k, the deepest this config serves — each alone and with a 5,500-word call between every turn. The third condition at 28k opens that call with the conversation's own system prompt, which is the other shape traffic beside a conversation comes in. `ingested_tokens` and `cached_tokens` are the server's own per-request accounting on the Messages path a Claude Code session takes, and each row carries the conversation that produced it, so the two depths cannot be averaged together. The server was restarted between conditions: it keeps prefixes it has evicted in host RAM, so a live one would serve the next condition from the last one's leftovers. Taken while a 54 GB download for 0017 held the disk: model loads between conditions were minutes rather than seconds, and `wall_seconds` carries that contention. The token columns do not — they are counts, and the per-turn ingest rate matched a run taken before the download started. |
| `2026-08-20-m2max-32gb-0018-session.jsonl` | 22 requests, 2 sessions | Every request two real `claude -p` sessions made on `config/agent.env`, accounted from the server's own log by `cmd/prefixlog`: prompt, ingested, reused, and how the slot was chosen. The two sessions ran back to back against one server and asked the same question, which is what makes row 14 readable — a second session's first request, against a server still holding the first's preamble. The prompt is derived rather than printed, so the instrument was held against the eviction file's rows first, where the endpoint's own account agrees on all 15 requests. Read-only tools over a copy of this repo, one question, and no calls beside the conversation at all — `CLAUDE_CODE_DISABLE_TERMINAL_TITLE` in the harness env suppresses them. |
| `2026-08-22-m2max-32gb-0024-screen.jsonl` | 9 screens | `--ubatch-size` walked up from llama.cpp's default of 512 at both profiles, each cell judged from a load and one prompt sized to the batch under test — a fixed smoke would dispatch a 512-token physical batch at every cell and find nothing. The ranges split: 32,768 admits 512–4096 and refuses 8192, 49,152 admits 512–2048 and refuses 4096. Both refuse at the same wired peak, 22.27 GB, with `kIOGPUCommandBufferCallbackErrorOutOfMemory` on the first prefill batch rather than at load — the server answers `/health` with 200 and then fails every command buffer, which is why `smoke` is the field that decides. `condition` is `unattended` throughout, so `desktop_verdict` is `not_applicable` and the attended verdict at these batch sizes is untaken. `smoke_seconds` is whole seconds and includes the eight-token reply, so it brackets the ingest rate rather than measuring it. |
| `2026-08-22-m2max-32gb-0024-fidelity.jsonl` | 9 probe runs | The gate on the depth sweep: fixed greedy prompts hashed at every admissible `--ubatch-size`, both profiles. A batch size changes how a prefill is split and floating-point reductions are not order-independent, so a moving hash would mean the faster cell answers differently rather than faster. `fidelity_probes` is `prefill` on every row and names the probe set — 6,000 and 12,000 tokens of the retrieval generator's haystack, long enough that each cell divides them into a different number of dispatches, answered with a paragraph rather than a twelve-token key. The short probe set 0017's rows carry fits in one physical batch at every cell and could only ever report a match. Each profile's first cell is run twice, on two loads, and the repeat is the control: a hash that cannot repeat itself at one batch size cannot report a divergence at another. Every cell matches its control. **Hashes are comparable within a profile only** — the two serve different contexts and different chat templates, so the difference between `f71ccec…` and `cc9e43a…` is not a batch-size finding. |
| `2026-08-22-m2max-32gb-0024-depth.jsonl` | 7 cells + 7 apparatus | Cold ingest at depth for every admissible `--ubatch-size`, each a `ladder.sh` rung at the profile's own context and KV type: 29,491 tokens at 32,768 and 44,236 at 49,152, one cold load per cell. `ubatch` and `batch` are on each row and in `cell`, because two rows called `ladder-32768-q8_0` cannot be told apart. `fill_seconds` is the column the decision rests on. **One pass, not two.** The second pass was started and stopped by hand, so no cell here has a repeat and the spread between two runs of one cell is unmeasured — what bounds the noise instead is that the grind profile's 1024, 2048 and 4096 cells agree within 2 s across three independent loads. Read the 512 cells against that cluster rather than against a repeat they do not have. `condition` is `unattended`, so `desktop_verdict` is `not_applicable` throughout. |
| `2026-08-22-m2max-32gb-0023-chain.jsonl` | apparatus + 3 sessions | One instruction carried across a chain of sessions, each budgeted and each ending with a handoff. Eight independent Go bugs, one per file, non-collapsible; progress read from `go test` and never from what a session claimed. Deliberately small: 16,384 served and declared, which leaves a 12,288 prompt budget once the 4,096 the harness keeps for a reply is counted, and about 2,800 tokens of room above the preamble. Sessions 1 and 2 were both denied at their ceiling mid-work, so no single session could have finished it. `peak_context_tokens` is the largest context any turn reached — the number the ceiling exists to keep under the window. |
| `2026-08-23-m2max-32gb-0025-screen.jsonl` | 1 screen | `config/driver-mtp-32k.env` judged from a load and a 29,491-token prompt — 90% of the context, the same fill 0017's `mtp-llamacpp-32k` row was taken at. Admissible, at a wired peak of 22.26 GB against that row's 22.10 for the same head without the driver's served defaults, which is the evidence that sampling and a chat-template override do not move the allocator. `condition` is `unattended`, so `desktop_verdict` is `not_applicable`; the compositor series behind the row sustains 0.24–0.59 cores, inside both attended thresholds. |
| `2026-08-23-m2max-32gb-0025-chain.jsonl` | 2 chains, 3 sessions | One instruction — twenty independent Go bugs, one per file — driven to completion on `config/agent.env` at 49,152 and on `config/driver-mtp-32k.env` at 32,768. **Scored by `go test`, never by what a session claimed**; `fixme_test.go` was byte-identical to a freshly generated one in both checkouts afterwards. The fixture comes from `scripts/chainfixture.py`, so the two repositories were identical by construction. `wall_seconds` is the column the decision rests on and it is the whole chain, model load excluded. `prompt_tokens_ingested` and `tokens_generated` are the server's own counters read either side of each run; `prompt_tokens_cached` is the prefix the server reused and is much the larger number at both contexts. **One run a side, no repeat**, at the served sampling — temperature 0.7, so the two chains did not do identical work and 46 tool calls against 35 is the size of that spread. Read the wall clock against `tokens_generated`, which agree within 5%. **The builds differ**, because the comparator is the shipped config: 49,152 was served by the `llama-server` on `PATH` and 32,768 by the PR #27342 build the MTP head needs, so the two rows differ by that as well as by context and mechanism. |
| `2026-08-23-m2max-32gb-0027-chain-account.jsonl` | 2 chains, 3 sessions | The two chains of `2026-08-23-m2max-32gb-0025-chain.jsonl` accounted a second way, by `localcode account` off the chains' own files — `sessions.jsonl` for the wall clock, `session.json` for the budget, and each session's transcript for everything else. **The overlap is the check**: `prompt_tokens_ingested` and `tokens_generated` agree with that file's server-side counters to the token on both chains, and `peak_context_tokens` with the supervisor's own rows. `record: chain` sums the sessions below it; the chain rows carry no `window_tokens`, since a window is a session's. `decode_per_second` and `prompt_per_second` are the **only fitted numbers here** — a transcript records no first-token time, so the two are a least-squares split of each call's clock between its prompt and its reply, and `rates_fitted` says so. Read them as a ratio rather than as rates: 8.41 tok/s against 6.44 is the 1.31x the MTP head is adopted for, arrived at from a different file, while either figure alone carries the harness's own time around the call. `model_seconds` against `wall_seconds` is the residual these were taken to settle — 5.2 s of 661 and 2.9 s of 825 outside a model call, so a chain's clock is its model calls and nothing else. **Not regenerable**: the transcripts and state directories behind it live outside the repository and were not kept. |

## Erratum: page size, 2026-08-18

`2026-08-17-m2max-32gb-ceiling-ladder.jsonl` was written by a `memprobe.sh` that assumed a
4 KB page. This machine pages at 16 KB, so in every row **`free_gb` and `compressed_gb` —
inside `before`, `loaded` and `filled` — and `free_at_peak_gb` are understated by exactly
4×.** Multiply, or re-derive from the raw counters.

`swap_used_mb` comes from `sysctl` and `llama_rss_gb` from `ps`; neither is affected, and
those are the fields the file's conclusions rest on. The rows are left as written rather
than rewritten in place: they are what the instrument recorded, and a results file that is
quietly edited after the fact is no longer evidence. The instrument is fixed going forward,
and rows carrying `wired_gb` are the ones taken after the fix.

## How these get here

`results/*.jsonl` in a worktree is **live scratch** and stays gitignored — it accumulates
across runs and gets deleted with the worktree. A snapshot is copied here when a feature
ships, which is the point at which numbers stop changing and start being cited.

## Reading them

Every table in [../TECH.md](../TECH.md) that one of these files backs can be regenerated:
`go run ./cmd/report -results <file> -baseline <label>` reproduces the harness comparison and
the decode ratios cell for cell. A table that no longer regenerates is a defect in one of the
two, and which one is the question it raises.

Rows are JSON Lines. Both files carry `condition`, so never aggregate without grouping by
it. Ladder rows also carry an `apparatus` record naming what else was resident, since a
condition label is a claim and that row is the evidence for it.
